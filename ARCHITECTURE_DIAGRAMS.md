# 📊 Architecture Diagrams

## Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          USER BROWSER                                            │
│                                                                                   │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │                    React Frontend (Port 3000)                              │  │
│  │                                                                             │  │
│  │  Components:                                                                │  │
│  │  • Login Page (Asgardeo integration)                                       │  │
│  │  • Admin Page (Gold theme)                                                 │  │
│  │  • User Page (Blue theme)                                                  │  │
│  │  • Protected Routes (RBAC)                                                 │  │
│  │                                                                             │  │
│  │  Storage:                                                                   │  │
│  │  • localStorage: JWT token, user info                                      │  │
│  └───────────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────┬──────────────────────────────────────────────┘
                                   │
                                   │ HTTP Requests
                                   │ Authorization: Bearer <JWT>
                                   │
            ┌──────────────────────┴──────────────────────┐
            │                                             │
            ▼                                             ▼
┌───────────────────────────┐                  ┌──────────────────────────┐
│   Auth Service            │                  │   Task Service           │
│   Port: 8080              │                  │   Port: 4000             │
│                           │                  │                          │
│ ┌───────────────────────┐ │                  │ ┌──────────────────────┐ │
│ │  REST Endpoints:      │ │                  │ │  REST Endpoints:     │ │
│ │  GET  /api/public     │ │                  │ │  GET    /health      │ │
│ │  GET  /api/private    │ │                  │ │  GET    /api/v1/tasks│ │
│ │  GET  /api/admin      │ │                  │ │  POST   /api/v1/tasks│ │
│ └───────────────────────┘ │                  │ │  GET    /tasks/:id   │ │
│                           │                  │ │  PATCH  /tasks/:id   │ │
│ ┌───────────────────────┐ │                  │ │  DELETE /tasks/:id   │ │
│ │  Middleware:          │ │                  │ └──────────────────────┘ │
│ │  • express-jwt        │ │                  │                          │
│ │  • JWKS validation    │ │                  │ ┌──────────────────────┐ │
│ │  • Role checking      │ │                  │ │  Middleware:         │ │
│ └───────────────────────┘ │                  │ │  • express-jwt       │ │
│                           │                  │ │  • JWKS validation   │ │
│ ┌───────────────────────┐ │                  │ │  • Role checking     │ │
│ │  Auth Config:         │ │                  │ │  • User extraction   │ │
│ │  • JWKS URI           │ │                  │ └──────────────────────┘ │
│ │  • Audience           │ │                  │                          │
│ │  • Issuer             │ │                  │ ┌──────────────────────┐ │
│ │  • Algorithm: RS256   │ │                  │ │  Controllers:        │ │
│ └───────────────────────┘ │                  │ │  • Task CRUD         │ │
└───────────┬───────────────┘                  │ │  • Statistics        │ │
            │                                  │ └──────────────────────┘ │
            │                                  │                          │
            │ OAuth 2.0                        │ ┌──────────────────────┐ │
            │ OpenID Connect                   │ │  Services:           │ │
            │                                  │ │  • Task Service      │ │
            ▼                                  │ │  • Event Publisher   │ │
┌───────────────────────────┐                  │ └──────────────────────┘ │
│      Asgardeo             │                  │                          │
│   OAuth 2.0 Provider      │                  │ ┌──────────────────────┐ │
│                           │                  │ │  gRPC Server:        │ │
│ • User Management         │                  │ │  Port: 50052         │ │
│ • JWT Token Issuing       │                  │ │  • GetTasksByProject │ │
│ • JWKS Endpoint           │                  │ │  • DeleteByProject   │ │
│ • Role Management         │                  │ └──────────────────────┘ │
│                           │                  └──────────┬───────────────┘
│ Organization:             │                             │
│ testforfinalproject       │                             │
│                           │                             │
│ Users:                    │                             │
│ • admin@demo.com          │                             │
│ • user@demo.com           │                   ┌─────────┴─────────┐
└───────────────────────────┘                   │                   │
                                                ▼                   ▼
                                    ┌──────────────────┐  ┌──────────────────┐
                                    │   PostgreSQL     │  │    RabbitMQ      │
                                    │   Port: 5432     │  │   Port: 5672     │
                                    │                  │  │                  │
                                    │ Tables:          │  │ Exchange:        │
                                    │ • tasks          │  │ • task_events    │
                                    │ • migrations     │  │                  │
                                    │                  │  │ Events:          │
                                    │ Columns:         │  │ • task.created   │
                                    │ • id             │  │ • task.updated   │
                                    │ • title          │  │ • task.deleted   │
                                    │ • description    │  │ • task.assigned  │
                                    │ • status         │  │                  │
                                    │ • priority       │  │                  │
                                    │ • assigned_to    │  │                  │
                                    │ • project_id     │  │                  │
                                    │ • created_at     │  │                  │
                                    │ • updated_at     │  │                  │
                                    └──────────────────┘  └──────────────────┘
```

---

## JWT Authentication Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Click "Sign In"
     ▼
┌─────────────────┐
│   Frontend      │
│  (Port 3000)    │
└────┬────────────┘
     │
     │ 2. Redirect to Asgardeo
     ▼
┌─────────────────────────────────┐
│        Asgardeo                 │
│  Login Page                     │
│  • Enter email/password         │
│  • MFA (if enabled)             │
└────┬────────────────────────────┘
     │
     │ 3. Validate credentials
     │ 4. Generate JWT token
     │ 5. Redirect back with token
     ▼
┌─────────────────┐
│   Frontend      │
│  • Store token  │
│  • Decode token │
│  • Extract roles│
└────┬────────────┘
     │
     │ 6. API Request
     │    Headers: { Authorization: "Bearer eyJhbGc..." }
     │
     ├─────────────────────────────┬──────────────────────────┐
     │                             │                          │
     ▼                             ▼                          ▼
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│ Auth Service │         │ Task Service │         │ Other Service│
│ (Port 8080)  │         │ (Port 4000)  │         │ (Future)     │
└──────┬───────┘         └──────┬───────┘         └──────┬───────┘
       │                        │                        │
       │ 7. Extract token       │ 7. Extract token       │ 7. Extract token
       │ 8. Validate with JWKS  │ 8. Validate with JWKS  │ 8. Validate with JWKS
       │ 9. Check signature     │ 9. Check signature     │ 9. Check signature
       │ 10. Check expiration   │ 10. Check expiration   │ 10. Check expiration
       │ 11. Check audience     │ 11. Check audience     │ 11. Check audience
       │ 12. Check issuer       │ 12. Check issuer       │ 12. Check issuer
       │                        │                        │
       ▼                        ▼                        ▼
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│  req.auth    │         │  req.auth    │         │  req.auth    │
│  {           │         │  {           │         │  {           │
│   sub: "..."│         │   sub: "..."│         │   sub: "..."│
│   email      │         │   email      │         │   email      │
│   roles      │         │   roles      │         │   roles      │
│  }           │         │  }           │         │  }           │
└──────┬───────┘         └──────┬───────┘         └──────┬───────┘
       │                        │                        │
       │ 13. Process request    │ 13. Process request    │ 13. Process request
       │     with user context  │     with user context  │     with user context
       │                        │                        │
       ▼                        ▼                        ▼
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   Response   │         │   Response   │         │   Response   │
└──────────────┘         └──────────────┘         └──────────────┘
```

---

## JWT Token Structure

```
eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6Ik...  ← HEADER
.
eyJzdWIiOiJ1c2VyLTEyMyIsImVtYWlsIjoidXNlckBkZW...  ← PAYLOAD
.
SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c     ← SIGNATURE

┌─────────────────────────────────────────────────────┐
│ HEADER (Base64 encoded)                             │
├─────────────────────────────────────────────────────┤
│ {                                                   │
│   "alg": "RS256",    ← Algorithm                    │
│   "typ": "JWT",      ← Token type                   │
│   "kid": "key-id"    ← Key ID for validation        │
│ }                                                   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ PAYLOAD (Base64 encoded)                            │
├─────────────────────────────────────────────────────┤
│ {                                                   │
│   "sub": "user-unique-id",        ← User ID         │
│   "email": "user@demo.com",       ← Email           │
│   "username": "user@demo.com",    ← Username        │
│   "given_name": "John",           ← First name      │
│   "family_name": "Doe",           ← Last name       │
│   "roles": "admin,user",          ← Roles           │
│   "aud": "KYEfJz...",             ← Audience        │
│   "iss": "https://api.asgardeo...",  ← Issuer       │
│   "exp": 1701878400,              ← Expiration      │
│   "iat": 1701874800               ← Issued at       │
│ }                                                   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ SIGNATURE (RSA-256 signed)                          │
├─────────────────────────────────────────────────────┤
│ RSASSA-PKCS1-v1_5 using SHA-256                     │
│                                                     │
│ Signed with Asgardeo's private key                  │
│ Verified with Asgardeo's public key (JWKS)          │
└─────────────────────────────────────────────────────┘
```

---

## Role-Based Access Control (RBAC)

```
┌─────────────────────────────────────────────────────────────────┐
│                      User Makes Request                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Authorization: Bearer <JWT>
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    JWT Middleware                                │
│  1. Validate token                                               │
│  2. Extract user info                                            │
│  3. Attach to request: req.auth                                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Extract Roles │
                    │  from req.auth │
                    └────────┬───────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌──────────────┐         ┌──────────────┐
        │  Admin Role  │         │  User Role   │
        │  "admin"     │         │  "user"      │
        └──────┬───────┘         └──────┬───────┘
               │                        │
               │                        │
        ┌──────┴────────────────────────┴──────┐
        │                                      │
        ▼                                      ▼

┌────────────────────────────────┐    ┌────────────────────────────┐
│      Admin Permissions          │    │     User Permissions        │
├────────────────────────────────┤    ├────────────────────────────┤
│ ✅ Create tasks                 │    │ ✅ Create tasks             │
│ ✅ Read all tasks               │    │ ✅ Read own tasks           │
│ ✅ Update all tasks             │    │ ✅ Update own tasks         │
│ ✅ Delete all tasks             │    │ ❌ Delete tasks             │
│ ✅ View all users               │    │ ❌ View all users           │
│ ✅ Manage roles                 │    │ ❌ Manage roles             │
│ ✅ Access admin dashboard       │    │ ✅ Access user dashboard    │
└────────────────────────────────┘    └────────────────────────────┘

Example Route Protection:

┌───────────────────────────────────────────────────────────────┐
│  router.delete('/tasks/:id',                                  │
│    checkJwt,              ← 1. Validate JWT                   │
│    extractUserId,         ← 2. Extract user ID                │
│    checkRole('admin'),    ← 3. Check admin role               │
│    deleteTask             ← 4. Execute if all checks pass     │
│  );                                                           │
└───────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Create Task Example

```
┌──────────┐
│  User    │ "I want to create a task"
└────┬─────┘
     │
     │ 1. Fill form (title, description, priority)
     │
     ▼
┌─────────────────────────────────────────────────┐
│  Frontend (React)                               │
│  • Get token from localStorage                  │
│  • Prepare request                              │
└────┬────────────────────────────────────────────┘
     │
     │ 2. POST http://localhost:4000/api/v1/tasks
     │    Headers: {
     │      Authorization: "Bearer eyJhbGc...",
     │      Content-Type: "application/json"
     │    }
     │    Body: {
     │      title: "Complete documentation",
     │      description: "Write API docs",
     │      priority: "high",
     │      status: "todo"
     │    }
     │
     ▼
┌─────────────────────────────────────────────────┐
│  Task Service (Express.js)                      │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ 3. Request hits middleware chain:        │  │
│  │                                          │  │
│  │    a) checkJwt                           │  │
│  │       ✓ Validate JWT signature           │  │
│  │       ✓ Check expiration                 │  │
│  │       ✓ Verify audience                  │  │
│  │       ✓ Verify issuer                    │  │
│  │       → Sets req.auth = {                │  │
│  │           sub: "user-123",               │  │
│  │           email: "user@demo.com",        │  │
│  │           roles: "user"                  │  │
│  │         }                                │  │
│  │                                          │  │
│  │    b) extractUserId                      │  │
│  │       → Sets req.userId = "user-123"     │  │
│  │                                          │  │
│  │    c) validate (Joi schema)              │  │
│  │       ✓ Check required fields            │  │
│  │       ✓ Validate data types              │  │
│  │       ✓ Check constraints                │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ 4. Controller: createTask                │  │
│  │    • Receives validated request           │  │
│  │    • Has user context in req.auth        │  │
│  │    • Calls service layer                 │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ 5. Service: taskService.createTask       │  │
│  │    • Add assigned_to = req.userId        │  │
│  │    • Insert into database                │  │
│  │    • Publish task.created event          │  │
│  └──────────────────────────────────────────┘  │
└────┬────────────────────────────────────────────┘
     │
     │ 6. Database Insert
     ▼
┌─────────────────────────────────────────────────┐
│  PostgreSQL                                     │
│  INSERT INTO tasks (                            │
│    id, title, description, priority,            │
│    status, assigned_to, created_at              │
│  ) VALUES (                                     │
│    'uuid-...', 'Complete documentation',        │
│    'Write API docs', 'high', 'todo',            │
│    'user-123', NOW()                            │
│  )                                              │
└────┬────────────────────────────────────────────┘
     │
     │ 7. Publish Event
     ▼
┌─────────────────────────────────────────────────┐
│  RabbitMQ                                       │
│  Exchange: task_events                          │
│  Routing Key: task.created                      │
│  Payload: {                                     │
│    taskId: "uuid-...",                          │
│    title: "Complete documentation",             │
│    assignedTo: "user-123",                      │
│    timestamp: "2025-12-06T..."                  │
│  }                                              │
└─────────────────────────────────────────────────┘
     │
     │ 8. Response
     ▼
┌─────────────────────────────────────────────────┐
│  Frontend receives:                             │
│  {                                              │
│    "success": true,                             │
│    "data": {                                    │
│      "id": "uuid-...",                          │
│      "title": "Complete documentation",         │
│      "description": "Write API docs",           │
│      "priority": "high",                        │
│      "status": "todo",                          │
│      "assignedTo": "user-123",                  │
│      "createdAt": "2025-12-06T...",             │
│      "updatedAt": "2025-12-06T..."              │
│    }                                            │
│  }                                              │
└─────────────────────────────────────────────────┘
     │
     │ 9. Update UI
     ▼
┌──────────┐
│  User    │ "Task created successfully!"
└──────────┘
```

---

## Service Independence & Scalability

```
┌─────────────────────────────────────────────────────────────┐
│             Horizontal Scaling Example                       │
└─────────────────────────────────────────────────────────────┘

Frontend (Single Instance)
┌─────────────────┐
│  Port: 3000     │
└────────┬────────┘
         │
         │ Load Balancer
         ▼
┌────────────────────────────────────────────────────────┐
│                    API Gateway                          │
│              (Optional - Future)                        │
└───┬────────────────────────────────────────────────┬───┘
    │                                                │
    │                                                │
    ▼                                                ▼
Auth Service Cluster                    Task Service Cluster
┌──────────┐ ┌──────────┐             ┌──────────┐ ┌──────────┐ ┌──────────┐
│Instance 1│ │Instance 2│             │Instance 1│ │Instance 2│ │Instance 3│
│Port: 8080│ │Port: 8081│             │Port: 4000│ │Port: 4001│ │Port: 4002│
└────┬─────┘ └────┬─────┘             └────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │                         │            │            │
     └────────┬───┘                         └──────┬─────┴────────────┘
              │                                    │
              │ All validate JWT                   │ All validate JWT
              │ independently                      │ independently
              │                                    │
              ▼                                    ▼
      ┌──────────────┐                    ┌──────────────────┐
      │  Asgardeo    │                    │   PostgreSQL     │
      │  (Shared)    │                    │  (Single/Cluster)│
      └──────────────┘                    └──────────────────┘

Benefits:
✅ Each service validates independently
✅ No shared session storage needed
✅ Can scale services separately
✅ Stateless = Easy horizontal scaling
✅ Load balancer can distribute requests
```

---

**All diagrams show the complete integrated architecture!** 🎉
